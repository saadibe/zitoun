{
  "name": "la-perla-pos",
  "version": "1.0.0",
  "description": "La Perla POS - Système de caisse restaurant",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
