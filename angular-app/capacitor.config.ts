import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.laperla.pos',
  appName: 'La Perla POS',
  webDir: 'dist/zitoun-angular/browser'
};

export default config;
